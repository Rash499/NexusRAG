# Example document

RAG combines retrieval with language generation. A retriever searches an
external knowledge base and supplies relevant context to a language model.
This makes the generated answer more grounded in the indexed corpus.
